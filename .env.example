# Host postgres user's UID, used by the one-shot migration container so peer
# auth over the mounted socket maps to the postgres role. Default is the Debian
# postgres package UID (999). Override if different: `id -u postgres`.
POSTGRES_UID=999
